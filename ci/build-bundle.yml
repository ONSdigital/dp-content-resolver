---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: onsdigital/dp-concourse-tools-git

inputs:
  - name: artifacts
  - name: dp-content-resolver

outputs:
  - name: build

run:
  path: dp-content-resolver/ci/scripts/build-bundle.sh
